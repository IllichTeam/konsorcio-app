/**
 * Preview entry for `pnpm email` (`email dev --dir emails`).
 * Canonical template used by the app lives in `src/emails/`.
 */
export {
  NotificacionConsorcio as default,
  NotificacionConsorcio,
  type NotificacionConsorcioProps,
} from "../src/emails/notificacion-consorcio";
